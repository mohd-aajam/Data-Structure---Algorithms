#include <bits/stdc++.h>
using namespace std;
 
int main () {
 int arrSize;
 cin>>arrSize;
 int array[arrSize];
 for(int i=0; i<arrSize; i++) {
     cin>>array[i];
 }
 
 sort(array,array+arrSize);

int min=array[0],max=array[arrSize-1];
int k;
cin>>k;
for(int i=0; i<arrSize; i++) {

}

return 0;
}